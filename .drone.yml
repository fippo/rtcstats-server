pipeline:
  restore-cache:
    image: drillster/drone-volume-cache
    restore: true
    ttl: 7
    mount:
      - /drone/docker
    volumes:
      - /tmp/cache:/cache
    when:
      event: [ tag ]
  build_docker:
    image: plugins/docker
    repo: andyet/rtcstats
    tags: 
      - ${DRONE_COMMIT_SHA:0:8}
      - ${DRONE_TAG##v}
    storage_path: /drone/docker
    secrets: [ docker_username, docker_password ]
    when:
      event: [ tag ]
  rebuild-cache:
    image: drillster/drone-volume-cache
    rebuild: true
    mount:
      - /drone/docker
    volumes:
      - /tmp/cache:/cache
    when:
      event: [ tag ]
  deploy:
    image: quay.io/honestbee/drone-kubernetes
    kubernetes_server: ${KUBERNETES_SERVER}
    kubernetes_cert: ${KUBERNETES_CERT}
    kubernetes_token: ${KUBERNETES_TOKEN}
    deployment: rtcstats-server
    repo: andyet/rtcstats
    container: rtcstats-server
    namespace: default
    tag: 
    - ${DRONE_TAG##v}
    when:
      event: tag
    secrets: [ kubernetes_cert, kubernetes_server, kubernetes_token ]
  slack:
    image: plugins/slack
    username: drone
    channel: alerts
    secrets: [ slack_webhook ]